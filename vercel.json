{
  "version": 2,
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}

{
  "rewrites": [
    { "source": "/apps/modo/:path*", "destination": "/api/modo/:path*" }
  ]
}
